<%= form_for(@hymn) do |f| %>
  <% if @hymn.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@hymn.errors.count, "error") %> prohibited this hymn from being saved:</h2>

      <ul>
      <% @hymn.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :title %><br>
    <%= f.text_field :title %>
  </div>
  <div class="field">
    <%= f.label :index %><br>
    <%= f.text_field :index %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
